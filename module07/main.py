# file -> .txt, .py, .zip, .json
# script -> bash, zsh, .py, shell
# module -> importable python files
# package -> modules + static files (images, .txt) + script

from utils import minutes_to_milliseconds


res = minutes_to_milliseconds(21)
print(res)